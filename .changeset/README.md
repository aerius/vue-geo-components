# Changesets

Each change adds one file here with the version bump and a changelog line. Add one with:

```bash
npm run changeset
```

At release time, `npm run version` uses these files to update the version and
`CHANGELOG.md`. See [../docs/versioning.md](../docs/versioning.md).
